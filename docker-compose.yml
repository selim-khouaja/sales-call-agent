services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file: .env

  n8n:
    image: n8nio/n8n:latest
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    environment:
      WEBHOOK_URL: "http://localhost:5678"

volumes:
  n8n_data:
